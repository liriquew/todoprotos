version: "3"

tasks:
  generate_sso:
    aliases:
      - gen_sso
    desc: "Generate code from proto files for sso"
    cmds:
      - protoc -I proto proto/sso/*.proto --go_out=./gen/go/ --go_opt=paths=source_relative --go-grpc_out=./gen/go/ --go-grpc_opt=paths=source_relative
